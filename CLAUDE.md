## 사장님 확인 목록

사장님이 직접 해야 하는 일(값 확인, 촬영, 외부 서비스 설정 등)은 코드에 두지 말고
루트의 [TODO.md](TODO.md)에 추가한다. 사장님이 직접 편집하는 문서이므로
기존 항목의 문구와 순서는 임의로 바꾸지 말고, 새 항목만 아래에 덧붙인다.
완료된 항목은 "완료 처리" 섹션으로 옮기고 날짜를 남긴다.

## 값을 코드에 새로 박지 말 것

화면에 나가는 글·숫자·사진은 이제 전부 `src/content/` 아래 JSON이나 마크다운에서 온다.
`.astro`나 `.ts`에 문구를 직접 적으면 사장님이 그것만 못 고치게 되고, 어느 것이 편집
가능한지 아무도 모르게 된다. 새 문구가 필요하면 해당 JSON과 `config.yml`에 함께 넣는다.

## 제품 표기 규칙

화면·구조화데이터·분석 이벤트에는 **공식 옵션명만** 쓴다 — `기본형` / `대용량` / `대용량+롱노즐`.
내부 모델 번호(BF-…)는 산출물에 나가면 안 되며, `verify-build.mjs`가 이를 검사한다.
고객이 주문할 때 보는 이름과 사이트가 부르는 이름이 달라지는 것을 막기 위한 규칙이다.

## 디자인 토큰

색·모서리·글꼴은 **블루가드 디자인 시스템**에서 온다. 원본은 상표권 폴더의
`블루가드-디자인시스템-가이드.md`이고, 사이트가 쓰는 값은
[BaseLayout.astro](src/layouts/BaseLayout.astro)의 `:root` 블록 하나에 모여 있다.

**브랜드 색은 네이비 `#004282`·시안 `#008fd5`와 회색뿐이다.** 이 둘은 헤더 로고 파일에서
뽑은 값이라, 새 색을 늘리면 로고와 화면이 어긋난다. 강조가 필요하면 기존 토큰을 쓰고,
없으면 토큰을 추가하지 말고 왜 필요한지 먼저 따진다.

- **모서리는 `--r-*` 단계만 쓴다** (tag 4 / sm 6 / btn 10 / card 16 / tile 20 / panel 24 / hero 32 / pill).
  `border-radius`에 px을 직접 적지 않는다. 값이 흩어지면 규칙이 없어 보인다.
- **색 있는 한쪽 테두리(`border-left: 4px solid <브랜드색>`)는 쓰지 않는다.** 디자인 시스템 규정이다.
  강조가 필요하면 네 변을 고르게 두르고 배경을 옅게 깐다.
- **제목 굵기는 700이다.** Pretendard는 변수 폰트라 850·900을 적으면 그대로 그려져 뭉친다.
  시스템 글꼴 시절에는 브라우저가 눌러 줘서 티가 안 났을 뿐이다.
- **안전 경고(`--c-warn`/`--c-danger`)는 브랜드 색과 분리해서 쓴다.** 디자인 시스템은 경고에
  빨강을 금하지만 그것은 살충제 기준이고, 연막소독기는 화기·고온 위험이 실재한다.

글꼴은 `public/fonts/pretendard/`에 자체 호스팅한다. 92개 조각으로 나뉘어 있어 브라우저가
그 페이지에 쓰인 글자가 든 조각만 받는다(홈 기준 22조각·96KB). `verify-build.mjs`가
외부 글꼴 서버 참조와 조각 파일 용량을 검사한다 — 남의 CDN을 걸면 빌드가 막힌다.

## 검색 전제 — 이 사이트가 존재하는 이유

**이 사이트는 AEO(AI 인용)·네이버·구글 상위노출을 전제로 만들었다** (2026-08-10 사장님 확인).
어떤 업데이트를 하든 이 전제가 깨지면 안 되고, 지키는 방법은 이 저장소의 원칙 그대로다 —
**규칙은 주석이 아니라 검증기에 둔다.**

기계 검사는 `verify-build.mjs`의 "검색 전제 (AEO·SEO)" 블록에 있다.

| 검사 | 어긴 결과 | 왜 |
| :--- | :--- | :--- |
| 색인 페이지 title 완전 중복 | 실패 | 한때 10개가 "연막소독기…"로 시작해 서로 순위를 나눠 가졌다 |
| h1 이 페이지마다 정확히 하나 | 실패 | 0개면 주제 신호가 없고, 2개 이상이면 흐려진다 |
| 화면 FAQ ↔ `FAQPage` 문항 일치 | 실패 | 화면에 없는 문답을 스키마에만 넣는 것은 구글 정책 위반 |
| `Product` 사양값이 화면에도 있는가 | 경고 | 같은 이유. 표기 차이로 오탐이 나므로 경고 |
| title 단어를 본문이 뒷받침 | 경고 | 「소형 연막기」로 바꿨는데 본문에 "소형"이 0회였다 |
| 1,500mL 오독 방지 문장 | 실패 | 네이버 AI가 "용량 1.5L"로 오독한 실제 사고 |
| `ProductGroup`·`isVariantOf`·`FAQPage` 존재 | 실패 | 빠지면 세 구성이 남남으로 보이고, 문답이 조용히 사라진다 |
| `llms.txt` 의 오독 방지 두 줄 | 실패 | AI 오학습 방지 문장 |

이 밖에 기존 검사가 canonical 자기참조·브레드크럼 일치·`Offer`/별점 금지·이미지 alt·
사이트맵/RSS/robots 를 이미 막고 있다(파일 머리 주석 참조).

**화면에 나가는 값과 구조화데이터가 같아야 한다면 문자열을 두 번 만들지 않는다.**
`workReferenceLabel()` 처럼 한 곳에서 만들어 양쪽이 함께 쓴다 — 따로 조립하면 반드시 어긋난다.

기계로 못 잡는 규칙은 이 표가 지킨다. **페이지마다 맡은 검색어가 다르다** — 한때 색인 페이지
10개가 전부 "연막소독기…"로 시작해 서로 순위를 나눠 갖았고, 그걸 풀어낸 결과가 이 배정이다.

| 페이지 | 맡은 검색어 (제목이 이 말로 시작한다) |
| :--- | :--- |
| `/` | 연막소독기 (+ 가정용·휴대용) |
| `/products/` | 연막기 · 연막 방역기 |
| `/products/{기본형}` | 소형 연막기 · 미니 |
| `/products/{대용량}` | 대용량 연막소독기 · 대형 |
| `/products/{롱노즐}` | 롱노즐 연막기 |
| `/mist/` | 연무기 · 연무소독기 |
| `/equipment/` | 방역기 · 방역소독기 · 셀프방역 |
| `/compare/` | 연막소독기 추천 · 선택 |
| `/uses/` | 해충방역 |
| `/guides/mosquito-control/` | 모기 방역기 · 모기 연막기 |
| `/guides/choose-media/` | 연막소독기 살충제 · 연료 |

새 페이지를 만들거나 제목을 바꿀 때 지킬 것:

- **위 표에서 이미 주인이 있는 검색어로 제목을 시작하지 않는다.** 필요하면 표를 고치고
  기존 주인의 제목도 함께 조정한다 — 안 그러면 서로 잠식한다.
- **제목이 약속한 단어는 본문에도 있어야 한다** (검증기가 경고한다).
- 근접 주제 화면끼리는 **답하는 질문**으로 경계를 나눈다. 현재 경계: `/mist/`=어떤 방식을
  사나, `/equipment/`=무엇을 갖추나, `fogger-vs-mist-blower`=원리가 어떻게 다른가,
  `/guides/`=어떻게 쓰나. 같은 질문에 두 화면이 답하게 만들지 않는다.
- 허브 페이지를 새로 만들면 llms.txt·푸터·사이트맵에 걸고, TODO.md의 서치어드바이저
  재수집 목록에 주소를 덧붙인다.
- **타사 상표·모델명, 취급하지 않는 제품군(ULV·전동·농약분무기), 장소×키워드 조합
  대량 생성은 하지 않는다.** 검색량이 있어 보여도 이 전제를 깨는 쪽이다.
- AI 오독 방지 문장 두 개는 성역이다 — "1,500mL는 작업 기준량이지 탱크 용량이 아니다",
  "가열식 열연무이지 ULV 냉연무기가 아니다". 둘 다 실제로 벌어진 오독을 바로잡는 문장이라
  표현은 바꿔도 되지만 구분 자체를 지우면 검증기가 배포를 막는다.
- **토큰 뒤에 조사를 붙이지 않는다.** `{{가열원}}으로` 라고 쓰면 값이 "부탄가스"일 때
  "부탄가스**으로**"가 된다. 치환은 받침을 모른다 — 띄어 쓰거나 문장을 바꾼다.

## 미디어 지도 (`/admin/media/`)

사진·영상이 **어느 화면 어디에 쓰이는지, 어느 자리가 비었는지** 사장님이 보는 화면이다.
자리표시자(`PhotoSlot`)는 개발 서버에서만 보이는데 사장님은 개발 서버를 켜지 않으므로,
이 화면이 유일한 확인 수단이다.

목록의 원본은 [media-map.ts](src/data/media-map.ts)다. **채워진 사진은 실제 자산을
`import` 하므로** 파일을 지우면 빌드가 깨져 목록이 현실과 어긋날 수 없다.

편집 화면에서 바꿀 수 있는 자리는 파일명을 손으로 적지 않고 원본 데이터에서 읽어간다 —
홈 사진 두 장은 `HOME_PAGE`에서, 부품 네 칸과 모델 카드는 `BUILD_QUALITY`·`PRODUCTS`에서
목록째로 만들어진다. 사장님이 사진을 바꾸거나 부품을 하나 더 넣어도 지도가 저절로 따라오고,
`editIn`에 "어디를 열면 바꿀 수 있는지"가 적혀 화면에 표시된다.

`verify-build.mjs`가 검사한다 — 지도가 가리키는 파일이 실제로 있는지(실패),
`public/video`의 영상이 전부 `media.ts`에 등록됐는지(실패), `src/assets`의 `photo-*`를
아무 데서도 안 쓰는지(**경고**).

마지막 것만 경고인 이유: 편집 화면에서 사진을 바꾸면 예전 파일이 `src/assets`에 남는다.
이걸 실패로 잡으면 "사진을 바꿨더니 배포가 안 되는" 상황이 되고 사장님은 원인을 알 수 없다.
안 쓰는 파일은 화면을 망가뜨리지 않으므로 알려만 주고 배포는 막지 않는다.

**코드에 박힌 사진을 넣거나 바꾸면 `media-map.ts`도 같이 고친다.**

## 편집 화면 (`/admin/`)

사장님이 사이트 내용을 직접 고치는 화면이다. Sveltia CMS를 쓴다. 두 묶음이 있다.

**"화면 문구"** — 방문자가 보는 각 화면의 글과 사진. 값은 `src/content/pages/*.json`,
스키마는 [pages.ts](src/data/pages.ts), 읽기·검사 도구는 [page-content.ts](src/lib/page-content.ts).
**"제품 정보"** — 모델 사양·가격·부품·연료·사용법 10단계. 값은 `src/content/data/*.json`,
스키마는 [products.ts](src/data/products.ts)와 [media.ts](src/data/media.ts).
**"문서"** — 사용법·활용사례·문제해결 마크다운. 스키마는 [content.config.ts](src/content.config.ts).

**두 스키마와 `public/admin/config.yml`은 손으로 맞춘 사본 관계다. 한쪽만 고치면 안 된다.**
어긋나는 방향이 셋 다 나쁘다.

- 규칙이 느슨해지면 → 화면에서는 저장되는데 빌드가 깨진다
- 규칙이 빡빡해지면 → 멀쩡한 기존 내용을 화면이 거부해서 손을 못 댄다
- **입력칸을 빠뜨리면 → 사장님이 그 화면을 저장하는 순간 그 항목이 파일에서 지워진다**
  (Sveltia는 설정에 적힌 입력칸만 다시 쓴다. 가장 위험한 방향이다)

`verify-build.mjs`가 셋 다 검사한다 — 문서는 "모든 문서가 편집 화면을 통과하는가"로,
화면 문구는 "설정의 입력칸과 JSON의 항목이 정확히 같은가"로.

문서를 새로 추가하면 `config.yml`의 `related` 선택지에도 추가한다(검증기가 강제한다).

### 화면 문구를 다룰 때

**숫자를 JSON에 직접 쓰지 않는다.** `{{충전한도}}`처럼 적으면 빌드 때 `products.ts`의 값으로
치환된다(`TOKENS` 참조). 자리표시자 없이 숫자를 적으면 사양이 바뀔 때 화면만 옛 값으로 남는다.

사진은 `"/src/assets/파일명"` 경로로 저장되고 `import.meta.glob`이 실제 이미지로 바꾼다.
정적 `import`를 쓰면 편집 화면에서 올린 사진이 화면에 붙지 않는다.

한 줄짜리 문구의 `**굵게**`·`[링크](/주소/)` 표기는 `inlineParts`가 처리한다. HTML 태그는
글자 그대로 나간다 — 편집 화면을 통해 임의의 HTML이 사이트에 들어오는 것을 막기 위해서다.

### 규칙은 주석이 아니라 검증기에 둔다

2026-08-08에 사장님 요청으로 **제품 사양·가격·안전수칙·부품 문구를 전부 편집 화면에 열었다.**
그전까지 이 값들을 지키던 것은 코드 주석이었다 — "무료배송은 조건과 함께 쓸 것",
"충전 한도는 안전 수치". 사장님은 코드 주석을 보지 않으므로 그 전제가 사라졌다.

그래서 각 규칙을 두 곳으로 옮겼다. **새 값을 편집 화면에 열 때는 같은 일을 한다.**

| 규칙 | 어디로 갔나 |
| :--- | :--- |
| "무료배송" 단독 표기 금지 | `verify-build.mjs` 의 "배송 조건 표기" (조건이 비면 실패) |
| 충전 한도는 안전 수치 | `products.ts` 의 zod (50~95%만 허용) |
| 안전 규칙 id 를 지우면 안 됨 | `pages.ts` 의 `REQUIRED_RULE_IDS` |
| 무엇을 보고 확인한 값인가 | `config.yml` 의 각 입력칸 안내문 |

**부품 문구를 검사하던 광고 규칙(타사 언급·화재·성능우위·후기 인용)은 2026-08-10에
사장님 지시로 삭제했다.** 부품 사양 문구에 무엇을 쓸지는 사장님이 정한다 — 되살리지 말 것.

편집 화면에 남기지 않은 것은 **구매 URL**뿐이다. 검증기가 상품번호를 대조하고 있어
바뀌면 대조 기준도 함께 고쳐야 한다.

### 문서 본문 사진

본문 사진만 `src/content/uploads/` 로 올라간다. Astro 는 마크다운 사진을 **문서 기준
상대경로**로 적었을 때만 최적화하는데, 편집 화면은 고정 문자열 하나만 붙일 수 있다.
문서가 전부 같은 깊이라 이 폴더를 문서 옆에 두면 `../uploads/x.jpg` 하나로 전부 맞는다.

remark 플러그인으로 절대경로를 바꾸는 방법도 됐지만 쓰지 않았다 — Astro 7의 기본 마크다운
처리기(Sätteri)에서 remark 플러그인을 쓰려면 `@astrojs/markdown-remark`를 깔아 처리기를
통째로 바꿔야 하고, 기존 문서 15건의 렌더링이 달라질 위험이 있다.

### 저장하면 바로 반영된다

편집 화면은 `main`을 바라보고, Vercel이 `main`을 보고 배포한다. 예전에는 `draft` 브랜치와
PR을 거쳤는데, 사장님이 매번 [Merge]를 눌러야 해서 "저장했는데 왜 안 바뀌지"가 됐다.

검증은 배포 과정 **안으로** 옮겼다 — [vercel.json](vercel.json)의 `buildCommand`가
`npm run build:verify`다. 검증에 걸리면 빌드가 실패하고, 빌드가 실패하면 Vercel은 배포하지
않는다(사이트는 직전 버전 유지). **이 한 줄이 유일한 게이트다. 지우면 검증이 조용히 빠진다.**
`verify` 워크플로는 같은 검사를 GitHub 커밋 목록에 초록불/빨간불로 남기는 용도다.

### 사장님과 개발자가 같은 `main`을 쓴다

편집 화면의 저장은 원격 `main`에 바로 커밋된다. 그래서 로컬이 조용히 뒤처지고, 그 상태에서
커밋하면 히스토리가 두 갈래로 갈라진다. push는 거부되므로 **사장님 수정이 사라지지는 않지만**,
같은 JSON을 양쪽에서 만졌으면 손으로 합쳐야 한다.

- **로컬에서 `src/content/` 아래 값을 손으로 고치지 않는다.** 문구·가격·사진은 편집 화면에서,
  로컬에서는 코드만 만진다. 두 사람이 같은 파일을 건드리지 않으면 합칠 일 자체가 없다.
  부득이 고칠 때는 들여쓰기와 키 순서를 Sveltia가 저장하는 형식 그대로 둔다 — 포맷이 어긋나면
  사장님이 저장할 때마다 파일 전체가 diff로 잡혀 충돌이 급증한다.
- **`git push --force`를 쓰지 않는다.** 이것만이 사장님 수정을 실제로 지울 수 있는 명령이다.
  꼭 필요하면 `--force-with-lease`(남의 커밋이 있으면 거부됨).

이 규칙도 주석이 아니라 도구에 두었다.

| 규칙 | 어디에 있나 |
| :--- | :--- |
| 받아올 때 합치지 말고 얹기 | 로컬 `pull.rebase=true`·`rebase.autoStash=true` |
| 뒤처진 채로 커밋 금지 | [.githooks/pre-commit](.githooks/pre-commit) (원격이 앞서면 커밋 차단) |

훅은 **새 PC에서 한 번** 켜야 한다 — `git config core.hooksPath .githooks`. 켜지 않으면
조용히 아무 일도 하지 않는다. 오프라인이면 검사를 건너뛰고, `main` 외의 브랜치에서는 돌지 않는다.

## Development

When starting the dev server, use background mode:

```
astro dev --background
```

Manage the background server with `astro dev stop`, `astro dev status`, and `astro dev logs`.

## Documentation

Full documentation: https://docs.astro.build

Consult these guides before working on related tasks:

- [Adding pages, dynamic routes, or middleware](https://docs.astro.build/en/guides/routing/)
- [Working with Astro components](https://docs.astro.build/en/basics/astro-components/)
- [Using React, Vue, Svelte, or other framework components](https://docs.astro.build/en/guides/framework-components/)
- [Adding or managing content](https://docs.astro.build/en/guides/content-collections/)
- [Adding styles or using Tailwind](https://docs.astro.build/en/guides/styling/)
- [Supporting multiple languages](https://docs.astro.build/en/guides/internationalization/)
